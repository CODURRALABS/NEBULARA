{
  "name": "@codurra/nebulara",
  "version": "1.0.0",
  "description": "Nebulara - Self-hosted native compiler",
  "type": "module",
  "scripts": {
    "bootstrap": "nasm -fwin64 VM/bootstrap.nasm -o bootstrap.exe",
    "native-run": "VM/bootstrap.exe example.nbs",
    "clean": "rm -rf build/*"
  },
  "dependencies": {}
}